* { margin: 0; padding: 0; box-sizing: border-box; }

  /* Light theme CSS variables (default) */
  :root {
    --bg: #fafbfc;
    --sidebar-bg: #ffffff;
    --surface: #ffffff;
    --surface-2: #f5f6f8;
    --border: #e6e8ec;
    --border-strong: #d1d4db;
    --fg: #1a1d26;
    --fg-secondary: #6b6f7a;
    --fg-tertiary: #9ca0ab;
    --accent: #4D6BFE;
    --accent-hover: #3d5bde;
    --accent-soft: rgba(77, 107, 254, 0.10);
    --accent-glow: rgba(77, 107, 254, 0.25);
    --hover: #f0f1f3;
    --selected: #e8eaf0;
    --blob-1: rgba(77, 107, 254, 0.12);
    --blob-2: rgba(123, 79, 254, 0.10);
    --topbar-bg: rgba(255,255,255,0.6);
    --shadow-sm: 0 2px 8px rgba(0,0,0,0.04);
    --shadow-md: 0 4px 24px rgba(0,0,0,0.06);
    --shadow-accent: 0 2px 8px rgba(77, 107, 254, 0.25);
    --scrollbar: #d4d4d4;
    --user-bubble: #4D6BFE;
    --ai-bubble: #f1f1f1;
  }

  [data-theme="dark"] {
    --bg: #0a0b0f;
    --sidebar-bg: #0d0e14;
    --surface: #14161d;
    --surface-2: #1a1d26;
    --border: #23262f;
    --border-strong: #2e323d;
    --fg: #f0f1f5;
    --fg-secondary: #a0a4ad;
    --fg-tertiary: #6b6f7a;
    --accent: #5b7cff;
    --accent-hover: #4d6bef;
    --accent-soft: rgba(91, 124, 255, 0.14);
    --accent-glow: rgba(91, 124, 255, 0.4);
    --hover: #1a1d26;
    --selected: #1f2230;
    --blob-1: rgba(91, 124, 255, 0.22);
    --blob-2: rgba(123, 79, 254, 0.14);
    --topbar-bg: rgba(10,11,15,0.6);
    --shadow-sm: 0 2px 8px rgba(0,0,0,0.3);
    --shadow-md: 0 4px 24px rgba(0,0,0,0.4);
    --shadow-accent: 0 2px 12px rgba(91, 124, 255, 0.35);
    --scrollbar: #2e323d;
    --user-bubble: #5b7cff;
    --ai-bubble: #1a1d26;
  }

  
  .bubble h1, .bubble h2, .bubble h3 { margin: 12px 0 6px; font-weight: 700; }
  .bubble p { margin-bottom: 8px; }
  .bubble p:last-child { margin-bottom: 0; }
  .bubble ul, .bubble ol { padding-left: 20px; margin-bottom: 8px; }
  .bubble pre { background: #1e1e1e; padding: 12px; border-radius: 8px; overflow-x: auto; margin: 8px 0; }
  .bubble code { font-family: monospace; font-size: 13px; }
  .bubble p code { background: rgba(77,107,254,0.1); padding: 2px 5px; border-radius: 4px; color: var(--accent); }
  .bubble blockquote { border-left: 3px solid var(--accent); padding-left: 12px; color: var(--fg-secondary); margin: 8px 0; }
  
  .mermaid-wrapper {
    background: var(--surface-2);
    border-radius: 8px;
    padding: 16px;
    margin: 12px 0;
    overflow: hidden;
    border: 1px solid var(--border);
    cursor: grab;
    position: relative;
    user-select: none;
    -webkit-user-select: none;
  }
  
  .mermaid-wrapper:active {
    cursor: grabbing;
  }
  
  .mermaid-wrapper svg {
    max-width: 100vw;
    height: 100vh;
    min-width: 50%;
    max-width: 50%;
    display: block;
    margin: 0 auto;
  }
  
  /* Zoom controls container */
  .mermaid-zoom-controls {
    position: absolute;
    top: 8px;
    right: 8px;
    display: flex;
    gap: 4px;
    opacity: 0;
    transition: opacity 0.2s ease;
    z-index: 5;
  }

  .mermaid-wrapper:hover .mermaid-zoom-controls {
    opacity: 1;
  }

  .zoom-btn {
    width: 28px;
    height: 28px;
    border-radius: 6px;
    border: 1px solid var(--border);
    background: var(--surface);
    color: var(--fg-secondary);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 11px;
    transition: all 0.15s;
    box-shadow: var(--shadow-sm);
  }
  .zoom-btn:hover {
    background: var(--hover);
    color: var(--accent);
    border-color: var(--accent);
  }

  /* Zoom level indicator */
  .mermaid-zoom-level {
    position: absolute;
    bottom: 8px;
    right: 8px;
    font-size: 11px;
    color: var(--fg-tertiary);
    background: var(--surface);
    padding: 2px 8px;
    border-radius: 4px;
    border: 1px solid var(--border);
    font-variant-numeric: tabular-nums;
    opacity: 0;
    transition: opacity 0.2s ease;
    pointer-events: none;
    z-index: 5;
  }
  .mermaid-wrapper:hover .mermaid-zoom-level {
    opacity: 1;
  }

  /* Tool call badge */
  .tool-call-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 12px;
    color: var(--fg-secondary);
    background: var(--surface-2);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 6px 12px;
    margin: 8px 0 4px;
    animation: fadeUp 0.3s ease-out;
  }
  .tool-call-badge i {
    color: var(--accent);
    font-size: 13px;
  }
  .tool-call-badge .tool-name {
    font-weight: 600;
    color: var(--accent);
  }

  /* Wikipedia result card */
  .wiki-card {
    background: var(--surface-2);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 16px;
    margin: 8px 0;
    transition: all 0.2s;
  }
  .wiki-card:hover {
    border-color: var(--accent);
    box-shadow: 0 2px 12px var(--accent-glow);
  }
  .wiki-card-title {
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 6px;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .wiki-card-title i {
    color: var(--accent);
    font-size: 14px;
  }
  .wiki-card-summary {
    font-size: 13.5px;
    color: var(--fg-secondary);
    line-height: 1.6;
    margin-bottom: 10px;
  }
  .wiki-card-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 12.5px;
    color: var(--accent);
    text-decoration: none;
    font-weight: 500;
    padding: 4px 10px;
    border-radius: 6px;
    background: var(--accent-soft);
    transition: all 0.15s;
  }
  .wiki-card-link:hover {
    background: var(--accent);
    color: white;
  }
  .wiki-card-sections {
    display: flex;
    flex-wrap: wrap;
    gap: 4px;
    margin-top: 8px;
  }
  .wiki-card-section-tag {
    font-size: 11px;
    color: var(--fg-tertiary);
    background: var(--surface);
    padding: 2px 8px;
    border-radius: 4px;
    border: 1px solid var(--border);
  }

  /* Wikipedia suggestions list */
  .wiki-suggestions {
    background: var(--surface-2);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 12px;
    margin: 8px 0;
  }
  .wiki-suggestion-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 8px 10px;
    border-radius: 8px;
    cursor: pointer;
    transition: background 0.15s;
    font-size: 13px;
    color: var(--fg);
  }
  .wiki-suggestion-item:hover {
    background: var(--hover);
  }
  .wiki-suggestion-item i {
    color: var(--fg-tertiary);
    font-size: 11px;
  }

  body {
    font-family: 'Plus Jakarta Sans', -apple-system, BlinkMacSystemFont, sans-serif;
    background: var(--bg);
    color: var(--fg);
    height: 100vh;
    overflow: hidden;
    -webkit-font-smoothing: antialiased;
    transition: background 0.4s ease, color 0.4s ease;
  }

  .app { display: flex; height: 100vh; }

  /* ---------- Sidebar ---------- */
  .sidebar {
    width: 260px;
    background: var(--sidebar-bg);
    border-right: 1px solid var(--border);
    display: flex;
    flex-direction: column;
    flex-shrink: 0;
    transition: transform 0.3s ease, background 0.4s ease, border-color 0.4s ease;
    position: relative;
    z-index: 10;
  }

  .sidebar-header {
    padding: 18px 16px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 10px;
    font-weight: 700;
    font-size: 15px;
    letter-spacing: -0.01em;
  }

  .brand-logo {
    width: 28px;
    height: 28px;
    border-radius: 8px;
    background: linear-gradient(135deg, #4D6BFE 0%, #7B4FFE 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 14px;
    font-weight: 800;
    box-shadow: 0 4px 12px var(--accent-glow);
  }

  .header-actions { display: flex; gap: 4px; }

  .icon-btn-sm {
    background: none;
    border: none;
    color: var(--fg-secondary);
    cursor: pointer;
    padding: 6px;
    border-radius: 6px;
    transition: all 0.2s;
    font-size: 14px;
  }
  .icon-btn-sm:hover { background: var(--hover); color: var(--fg); }

  .new-chat-btn {
    margin: 4px 12px 16px;
    padding: 10px 14px;
    background: var(--accent);
    color: white;
    border: none;
    border-radius: 10px;
    font-family: inherit;
    font-size: 13.5px;
    font-weight: 600;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: space-between;
    transition: all 0.2s;
    box-shadow: var(--shadow-accent);
  }
  .new-chat-btn:hover {
    background: var(--accent-hover);
    transform: translateY(-1px);
    box-shadow: 0 6px 18px var(--accent-glow);
  }

  .history-section {
    flex: 1;
    overflow-y: auto;
    padding: 0 8px;
  }
  .history-section::-webkit-scrollbar { width: 4px; }
  .history-section::-webkit-scrollbar-thumb { background: var(--scrollbar); border-radius: 2px; }

  .history-group-label {
    font-size: 11px;
    font-weight: 600;
    color: var(--fg-tertiary);
    padding: 14px 8px 6px;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .history-item {
    padding: 9px 10px;
    border-radius: 8px;
    font-size: 13px;
    color: var(--fg-secondary);
    cursor: pointer;
    transition: all 0.15s;
    display: flex;
    align-items: center;
    gap: 9px;
    margin-bottom: 1px;
    white-space: nowrap;
    overflow: hidden;
  }
  .history-item:hover { background: var(--hover); color: var(--fg); }
  .history-item.active { background: var(--selected); color: var(--fg); font-weight: 500; }
  .history-item .icon { font-size: 11px; color: var(--fg-tertiary); flex-shrink: 0; }
  .history-item .text { overflow: hidden; text-overflow: ellipsis; flex: 1; }

  .sidebar-footer {
    padding: 12px;
    border-top: 1px solid var(--border);
    position: relative;
  }

  .user-card {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px;
    border-radius: 10px;
    cursor: pointer;
    transition: background 0.15s;
  }
  .user-card:hover { background: var(--hover); }

  .avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: linear-gradient(135deg, #ff6b6b 0%, #ee5a6f 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 12px;
    font-weight: 700;
    flex-shrink: 0;
    letter-spacing: 0.02em;
  }

  .user-info { flex: 1; min-width: 0; }
  .user-name { font-size: 13px; font-weight: 600; color: var(--fg); }
  .user-plan { font-size: 11px; color: var(--fg-tertiary); margin-top: 1px; }

  /* Profile Dropdown */
  .profile-dropdown {
    position: absolute;
    bottom: 70px;
    left: 12px;
    right: 12px;
    background: var(--surface);
    border: 1px solid var(--border-strong);
    border-radius: 12px;
    box-shadow: var(--shadow-md);
    padding: 6px;
    opacity: 0;
    visibility: hidden;
    transform: translateY(10px);
    transition: all 0.2s ease;
    z-index: 20;
  }

  .profile-dropdown.show {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }
  .dropdown-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 9px 10px;
    border-radius: 8px;
    font-size: 13px;
    color: var(--fg-secondary);
    cursor: pointer;
    transition: background 0.15s;
  }
  .dropdown-item:hover { background: var(--hover); color: var(--fg); }
  .dropdown-item.danger:hover { background: rgba(255,80,80,0.1); color: #ff5050; }
  .dropdown-item i { width: 16px; text-align: center; }

  /* ---------- Main chat area ---------- */
  .chat-area {
    flex: 1;
    display: flex;
    flex-direction: column;
    background: var(--bg);
    position: relative;
    overflow: hidden;
    min-width: 0;
    transition: background 0.4s ease;
  }

  .chat-bg { position: absolute; inset: 0; pointer-events: none; overflow: hidden; z-index: 0; }
  .blob { position: absolute; border-radius: 50%; filter: blur(80px); transition: background 0.4s ease; }
  .blob-1 { width: 480px; height: 480px; background: radial-gradient(circle, var(--blob-1), transparent 70%); top: -160px; right: -120px; animation: float 22s ease-in-out infinite; }
  .blob-2 { width: 380px; height: 380px; background: radial-gradient(circle, var(--blob-2), transparent 70%); bottom: -140px; left: 15%; animation: float 28s ease-in-out infinite reverse; }
  @keyframes float { 0%, 100% { transform: translate(0, 0) scale(1); } 50% { transform: translate(40px, 30px) scale(1.05); } }

  .top-bar {
    padding: 14px 24px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-bottom: 1px solid var(--border);
    position: relative;
    z-index: 2;
    background: var(--topbar-bg);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    transition: border-color 0.4s ease;
  }

  .top-bar-left { display: flex; align-items: center; gap: 8px; }
  .menu-btn { background: none; border: none; color: var(--fg-secondary); cursor: pointer; padding: 8px; border-radius: 8px; display: none; font-size: 15px; }
  .menu-btn:hover { background: var(--hover); color: var(--fg); }
  .top-bar-title { font-size: 14px; color: var(--fg-secondary); font-weight: 500; }
  .top-bar-title strong { color: var(--fg); font-weight: 600; }
  .top-bar-right { display: flex; align-items: center; gap: 8px; }

  .pill-btn {
    background: none;
    border: 1px solid var(--border);
    color: var(--fg-secondary);
    cursor: pointer;
    padding: 7px 12px;
    border-radius: 8px;
    font-size: 12.5px;
    font-weight: 500;
    display: flex;
    align-items: center;
    gap: 6px;
    transition: all 0.15s;
    font-family: inherit;
  }
  .pill-btn:hover { background: var(--hover); color: var(--fg); border-color: var(--border-strong); }

  .theme-toggle {
    position: relative;
    width: 38px;
    height: 38px;
    border-radius: 8px;
    border: 1px solid var(--border);
    background: none;
    cursor: pointer;
    overflow: hidden;
    transition: all 0.2s;
    color: var(--fg-secondary);
    font-size: 14px;
    display: grid;
    place-items: center;
  }
  .theme-toggle:hover { background: var(--hover); color: var(--accent); border-color: var(--border-strong); }
  .theme-toggle .icon-sun, .theme-toggle .icon-moon { grid-area: 1 / 1; transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1), opacity 0.3s, color 0.3s; }
  .theme-toggle .icon-moon { transform: scale(0) rotate(40deg); opacity: 0; }
  .theme-toggle .icon-sun { transform: scale(1) rotate(0); opacity: 1; }
  [data-theme="dark"] .theme-toggle .icon-moon { transform: scale(1) rotate(0); opacity: 1; color: #ffd76b; }
  [data-theme="dark"] .theme-toggle .icon-sun { transform: scale(0) rotate(-40deg); opacity: 0; }

  /* ---------- Chat content & Messages ---------- */
  .chat-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 0 24px;
    position: relative;
    z-index: 1;
    overflow: hidden;
  }

  .chat-scroll {
    width: 100%;
    max-width: 100%;
    padding: 40px 0 20px;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    gap: 24px;
    scroll-behavior: smooth;
  }
  .chat-scroll::-webkit-scrollbar { width: 6px; }
  .chat-scroll::-webkit-scrollbar-thumb { background: var(--scrollbar); border-radius: 3px; }

  .greeting {
    text-align: center;
    margin: auto 0;
    animation: fadeUp 0.6s ease-out;
  }
  @keyframes fadeUp { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: translateY(0); } }

  .greeting h1 {
    font-size: 32px;
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-bottom: 8px;
    background: linear-gradient(135deg, var(--fg) 0%, var(--fg-secondary) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  [data-theme="dark"] .greeting h1 { background: linear-gradient(135deg, #ffffff 0%, #8b9bbf 100%); -webkit-background-clip: text; background-clip: text; }
  .greeting p { color: var(--fg-secondary); font-size: 15px; }

  /* Message Bubbles */
  .message-wrapper {
    display: flex;
    flex-direction: column;
    max-width: 85%;
    animation: msgPop 0.3s ease-out;
  }
  @keyframes msgPop { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

  .message-wrapper.user { align-self: flex-end; align-items: flex-end; padding-right: 170px; padding-left: 210px; }
  .message-wrapper.ai { align-self: flex-start; align-items: flex-start; padding-left: 150px; padding-right: 30px; }

  .msg-sender { font-size: 11px; color: var(--fg-tertiary); margin-bottom: 6px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.04em; }
  
  .bubble {
    padding: 12px 16px;
    border-radius: 16px;
    line-height: 1.5;
    font-size: 14.5px;
    word-wrap: break-word;
    width: 100%;
  }
  .message-wrapper.user .bubble {
    background: var(--user-bubble);
    color: white;
    border-bottom-right-radius: 4px;
    box-shadow: 0 2px 8px rgba(77, 107, 254, 0.2);
  }
  .message-wrapper.ai .bubble {
    color: var(--fg);
    border-bottom-left-radius: 4px;
  }

  /* Typing Indicator */
  .typing-indicator { display: flex; gap: 4px; padding: 14px 18px; background: var(--ai-bubble); border: 1px solid var(--border); border-radius: 16px; border-bottom-left-radius: 4px; width: fit-content; }
  .typing-dot { width: 7px; height: 7px; background: var(--fg-tertiary); border-radius: 50%; animation: typing 1.4s infinite ease-in-out; }
  .typing-dot:nth-child(2) { animation-delay: 0.2s; }
  .typing-dot:nth-child(3) { animation-delay: 0.4s; }
  @keyframes typing { 0%, 60%, 100% { transform: translateY(0); opacity: 0.4; } 30% { transform: translateY(-5px); opacity: 1; } }

  /* ---------- Input Footer ---------- */
  .chat-footer {
    width: 100%;
    max-width: 760px;
    margin: 0 auto;
    padding: 10px 0 30px;
    margin-top: auto;
  }

  .input-container {
    width: 100%;
    background: var(--surface);
    border: 1px solid var(--border-strong);
    border-radius: 24px;
    padding: 14px 16px 10px;
    transition: all 0.2s;
    box-shadow: var(--shadow-md);
  }
  .input-container:focus-within { border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-soft), var(--shadow-md); }

  .input-textarea {
    width: 100%;
    border: none;
    outline: none;
    resize: none;
    font-family: inherit;
    font-size: 15px;
    color: var(--fg);
    background: transparent;
    max-height: 150px;
    line-height: 1.5;
    padding: 2px 0;
  }
  .input-textarea::placeholder { color: var(--fg-tertiary); }

  .input-actions { display: flex; align-items: center; justify-content: flex-end; margin-top: 8px; }

  .send-btn {
    width: 32px;
    height: 32px;
    border-radius: 8px;
    background: var(--accent);
    color: white;
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s;
    font-size: 13px;
  }
  .send-btn:hover:not(:disabled) { background: var(--accent-hover); transform: scale(1.05); box-shadow: 0 4px 12px var(--accent-glow); }
  .send-btn:disabled { background: var(--border-strong); color: var(--fg-tertiary); cursor: not-allowed; }

  .disclaimer { text-align: center; margin-top: 14px; font-size: 11px; color: var(--fg-tertiary); }

  /* ---------- Toast ---------- */
  .toast {
    position: fixed;
    bottom: 30px;
    left: 50%;
    transform: translateX(-50%) translateY(100px);
    background: var(--fg);
    color: var(--bg);
    padding: 10px 18px;
    border-radius: 10px;
    font-size: 13px;
    font-weight: 500;
    z-index: 1000;
    transition: transform 0.3s ease;
    box-shadow: 0 8px 24px rgba(0,0,0,0.2);
    max-width: 90vw;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .toast.show { transform: translateX(-50%) translateY(0); }

  /* ---------- Modals ---------- */
  .modal-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.6);
    backdrop-filter: blur(4px);
    display: none;
    align-items: center;
    justify-content: center;
    z-index: 100;
    opacity: 0;
    transition: opacity 0.2s;
  }
  .modal-overlay.show { display: flex; opacity: 1; }

  .modal {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 16px;
    width: 90%;
    max-width: 400px;
    box-shadow: 0 24px 64px rgba(0,0,0,0.2);
    transform: scale(0.95);
    transition: transform 0.2s;
    overflow: hidden;
  }
  .modal-overlay.show .modal { transform: scale(1); }

  .modal-header { padding: 20px 24px; border-bottom: 1px solid var(--border); display: flex; justify-content: space-between; align-items: center; }
  .modal-title { font-size: 16px; font-weight: 700; }
  .modal-body { padding: 20px 24px; }
  .modal-footer { padding: 16px 24px; border-top: 1px solid var(--border); display: flex; justify-content: flex-end; gap: 8px; }

  .settings-row { display: flex; justify-content: space-between; align-items: center; padding: 12px 0; border-bottom: 1px solid var(--border); }
  .settings-row:last-child { border-bottom: none; }
  .settings-info h4 { font-size: 14px; font-weight: 600; margin-bottom: 2px; }
  .settings-info p { font-size: 12px; color: var(--fg-secondary); }

  /* Toggle Switch */
  .switch { position: relative; display: inline-block; width: 40px; height: 22px; }
  .switch input { opacity: 0; width: 0; height: 0; }
  .slider { position: absolute; cursor: pointer; inset: 0; background: var(--border-strong); border-radius: 22px; transition: 0.3s; }
  .slider:before { position: absolute; content: ""; height: 16px; width: 16px; left: 3px; bottom: 3px; background: white; border-radius: 50%; transition: 0.3s; }
  input:checked + .slider { background: var(--accent); }
  input:checked + .slider:before { transform: translateX(18px); }

  .btn { padding: 8px 16px; border-radius: 8px; border: 1px solid var(--border-strong); background: var(--surface-2); color: var(--fg); font-family: inherit; font-size: 13px; font-weight: 600; cursor: pointer; transition: all 0.2s; }
  .btn:hover { background: var(--hover); }
  .btn.primary { background: var(--accent); color: white; border-color: var(--accent); }
  .btn.primary:hover { background: var(--accent-hover); }
  .btn.danger { background: #ff4757; color: white; border-color: #ff4757; }
  .btn.danger:hover { background: #e03e4d; }

  /* ---------- Logout Screen ---------- */
  .logout-overlay {
    position: fixed;
    inset: 0;
    background: var(--bg);
    z-index: 200;
    display: none;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 24px;
  }
  .logout-overlay.show { display: flex; }
  .logout-icon { width: 60px; height: 60px; background: var(--accent-soft); color: var(--accent); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 24px; margin-bottom: 20px; }
  .logout-overlay h2 { font-size: 22px; margin-bottom: 8px; }
  .logout-overlay p { color: var(--fg-secondary); margin-bottom: 24px; }

  /* ---------- Mobile ---------- */
  .sidebar-overlay { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.5); z-index: 9; opacity: 0; transition: opacity 0.3s; }
  .sidebar-overlay.show { opacity: 1; }

  @media (max-width: 768px) {
    .sidebar { position: fixed; left: -260px; top: 0; height: 100vh; z-index: 10; }
    .sidebar.open { left: 0; }
    .menu-btn { display: block; }
    .greeting h1 { font-size: 24px; }
    .top-bar { padding: 12px 16px; }
    .message-wrapper { max-width: 95%; }
  }

  .sidebar, .top-bar, .input-container, .pill-btn, .history-item, .user-card, .theme-toggle, .brand-logo, .new-chat-btn, .send-btn, .avatar, .bubble { transition-property: background, color, border-color, box-shadow; transition-duration: 0.3s; transition-timing-function: ease; }

  /* ---------- Model Selector Dropdown ---------- */
  .model-selector {
    position: relative;
    display: flex;
    align-items: center;
  }

  #modelSelect {
    appearance: none;
    -webkit-appearance: none;
    background: var(--surface);
    border: 1px solid var(--border);
    color: var(--fg);
    padding: 7px 30px 7px 12px;
    border-radius: 8px;
    font-family: inherit;
    font-size: 12.5px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.15s;
    outline: none;
    min-width: 220px;
    max-width: 260px;
  }

  #modelSelect:hover {
    background: var(--hover);
    border-color: var(--border-strong);
  }

  #modelSelect:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 3px var(--accent-soft);
  }

  .model-selector::after {
    content: '\f107';
    font-family: 'Font Awesome 6 Free';
    font-weight: 900;
    position: absolute;
    right: 10px;
    top: 50%;
    transform: translateY(-50%);
    pointer-events: none;
    color: var(--fg-tertiary);
    font-size: 11px;
  }

/* Tavily Result Card */
.tavily-card {
  background: var(--bg-secondary);
  border-left: 3px solid #6c5ce7;
  border-radius: 8px;
  padding: 12px 16px;
  margin: 10px 0;
  box-shadow: 0 2px 6px rgba(0,0,0,0.08);
}

.tavily-card-title {
  font-weight: 600;
  font-size: 14px;
  color: var(--fg-primary);
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 6px;
}

.tavily-card-title i {
  color: #6c5ce7;
}

.tavily-card-answer {
  background: var(--bg-accent);
  padding: 8px 12px;
  border-radius: 6px;
  font-size: 13px;
  color: var(--fg-secondary);
  margin: 8px 0;
  border-left: 2px solid #6c5ce7;
}

.tavily-card-results {
  display: flex;
  flex-direction: column;
  gap: 6px;
  margin-top: 8px;
}

.tavily-result-item {
  display: flex;
  align-items: baseline;
  gap: 8px;
  font-size: 13px;
  padding: 4px 0;
}

.tavily-result-item .tavily-badge {
  font-size: 10px;
  background: #6c5ce7;
  color: #fff;
  padding: 1px 6px;
  border-radius: 10px;
  font-weight: 600;
}

.tavily-result-item a {
  color: var(--link-color);
  text-decoration: none;
  font-weight: 500;
}

.tavily-result-item a:hover {
  text-decoration: underline;
}

.tavily-result-item .tavily-snippet {
  color: var(--fg-tertiary);
  font-size: 12px;
  display: block;
  margin-top: 2px;
}